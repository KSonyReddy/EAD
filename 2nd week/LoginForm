<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Login Form with Validation</title>
<style>
  * { box-sizing: border-box; }
  body {
    margin: 0; height: 100vh;
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    display: flex; justify-content: center; align-items: center;
    background: url('https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTBlS2V_aUE3eb02MlYSJUpEQZoupb7xwVL5g&s') repeat-x center/cover fixed;
    animation: bgMove 30s linear infinite;
    overflow: hidden;
  }
  @keyframes bgMove {
    0% { background-position: 0 50%; }
    100% { background-position: 100% 50%; }
  }
  .login-form {
    background: rgba(255 255 255 / 0.9);
    padding: 40px 35px;
    border-radius: 16px;
    box-shadow: 0 8px 24px rgba(0 0 0 / 0.12);
    width: 340px;
    backdrop-filter: blur(10px);
    transition: box-shadow 0.3s ease;
  }
  .login-form:hover {
    box-shadow: 0 12px 40px rgba(0 0 0 / 0.16);
  }
  h2 {
    margin-bottom: 30px; font-weight: 700; font-size: 26px;
    text-align: center; color: #00796b; letter-spacing: 1.1px;
  }
  label {
    display: block; margin-bottom: 8px;
    color: #004d40; font-weight: 600; font-size: 14px;
  }
  input {
    width: 100%; padding: 14px 16px; margin-bottom: 25px;
    border: 2px solid #b2dfdb; border-radius: 12px;
    font-size: 15px; color: #004d40; background: #e0f2f1;
    font-family: inherit;
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.06);
    transition: border-color 0.25s ease, background-color 0.25s ease;
  }
  input::placeholder {
    color: #00796b; opacity: 0.7;
  }
  input:focus {
    border-color: #00796b; background: #fff;
    outline: none; box-shadow: 0 0 8px rgba(0,121,107,0.3);
  }
  .button-group {
    display: flex; gap: 15px; justify-content: center;
  }
  button {
    flex: 1; padding: 15px 0; border-radius: 14px;
    font-size: 17px; font-weight: 700; cursor: pointer;
    box-shadow: 0 6px 12px rgba(0,121,107,0.35);
    border: none; transition: background-color 0.3s ease, box-shadow 0.3s ease;
  }
  button[type="submit"] {
    background-color: #00796b; color: white;
  }
  button[type="submit"]:hover {
    background-color: #004d40;
    box-shadow: 0 8px 20px rgba(0,77,64,0.45);
  }
  button[type="submit"]:active {
    transform: translateY(2px);
    box-shadow: 0 4px 10px rgba(0,77,64,0.45);
  }
  button[type="reset"] {
    background-color: #b0bec5; color: #263238;
    box-shadow: 0 6px 12px rgba(176,190,197,0.5);
  }
  button[type="reset"]:hover {
    background-color: #90a4ae;
    box-shadow: 0 8px 20px rgba(144,164,174,0.7);
  }
  button[type="reset"]:active {
    transform: translateY(2px);
    box-shadow: 0 4px 10px rgba(144,164,174,0.7);
  }
</style>
</head>
<body>

<div class="login-form">
  <h2>Login</h2>
  <form id="loginForm" onsubmit="return validateLogin()">
    <label for="username">Username</label>
    <input type="text" id="username" name="username" placeholder="Enter username" />

    <label for="password">Password</label>
    <input type="password" id="password" name="password" placeholder="Enter password" />

    <div class="button-group">
      <button type="submit">Login</button>
      <button type="reset">Reset</button>
    </div>
  </form>
</div>

<script>
  function validateLogin() {
    const username = document.getElementById('username').value.trim();
    const password = document.getElementById('password').value.trim();

    if (!username) {
      alert('Please enter your username.');
      return false;
    }
    if (!password) {
      alert('Please enter your password.');
      return false;
    }

    const validUsername = 'sony';
    const validPassword = 'sony';

    if (username === validUsername && password === validPassword) {
      alert('Login successful!');
      return true;
    } else {
      alert('Login failed. Invalid username or password.');
      return false;
    }
  }
</script>

</body>
</html>
